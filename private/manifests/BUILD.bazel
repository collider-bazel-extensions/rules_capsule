package(default_visibility = ["//visibility:public"])

# Pre-rendered Capsule install YAMLs. Generated by tools/render_capsule.sh
# and committed (decision #4 in DESIGN.md). Sha256s tracked in
# private/versions.bzl; verified at maintainer render time.
exports_files(glob(["*.yaml"]))
